# 格物维护交接

<p>
  <a href="./HANDOFF.md"><strong>简体中文</strong></a>
  ·
  <a href="./HANDOFF_EN.md">English</a>
</p>

本文面向维护者，说明格物的产品合同、仓库结构、验证方式、发布边界和当前待办。用户上手请阅读 [`docs/GETTING_STARTED.md`](./docs/GETTING_STARTED.md)。

## 当前状态

- 当前插件版本：`1.1.0-rc.2`
- 当前分支：`main`
- 分发方式：GitHub 仓库市场 + 可移植 Skill 目录
- 通用插件目录：尚未提交
- GitHub Release：尚未发布
- 静态校验：通过
- 1.1 真实前向测试：六项关键场景已通过，其余扩展场景待执行
- 独立安装：已经在另一台电脑验证成功

不要把“结构校验通过”“插件能安装”和“真实调查行为通过”合并为一个状态。

市场清单中的 `policy.authentication: "ON_INSTALL"` 是插件市场要求的安装策略元数据。格物没有 Apps、MCP Server 或 OAuth 流程，因此不应出现格物自己的账号授权步骤。

## 产品合同

格物必须守住以下合同：

1. 显式调用后才进入完整调查工作流
2. 默认只读，研究不自动授权执行第三方代码或修改外部系统
3. 优先一手来源，并区分观察、声称、验证、推断和未知
4. 只有完成决策关键材料的验收，才能声明完整审阅
5. 受阻时明确降级，不跨浏览器表面绕过安全策略
6. 调查结论优先回答下一步行动，不自动进入产品化或实施
7. 来源反查止于公开项目、产品、仓库和发布者

更细的执行规则以 [`skills/gewu/SKILL.md`](./skills/gewu/SKILL.md) 和其直接引用的 `references/` 为准。不要把用户文档全文复制进 Skill。

## 仓库结构

```text
.
├── .agents/plugins/             # GitHub 仓库市场元数据
├── .codex-plugin/plugin.json    # 插件清单
├── assets/                      # README、社交图和真实安装截图
├── docs/                        # 用户上手与排错
├── evals/                       # 前向测试场景与结果
├── scripts/                     # 静态校验和反向回归
├── skills/gewu/                 # 可移植运行核心
├── PRODUCT.md                   # 产品定义与边界
├── CHANGELOG.md                 # 用户可见版本演进
└── HANDOFF.md                   # 维护交接
```

中英文文件使用 `_EN` 后缀配对。新增用户文档时，应决定是否需要同步英文版，不要在一个文件里混写两种语言。

## 修改哪一层

| 变化 | 修改位置 |
| --- | --- |
| 调查步骤、授权或安全合同 | `skills/gewu/SKILL.md` |
| 特定材料路线或详细判断规则 | `skills/gewu/references/` |
| 确定性校验 | `scripts/` |
| 真实行为证据 | `evals/` |
| 安装、上手和排错 | `docs/` 与 README |
| 插件展示和分发元数据 | `.codex-plugin/`、`.agents/plugins/` |
| 产品定位与非目标 | `PRODUCT.md` |
| 版本变化 | `CHANGELOG.md` |

避免在多个运行文件重复同一条规则。核心合同留在 `SKILL.md`，详细路线只放在一个 reference 中。

## 本地验证

### 1. 格物自带校验

```bash
python3 scripts/validate_skill.py
python3 scripts/test_validate_skill.py
```

### 2. Skill 结构校验

```bash
python3 <path-to-skill-creator>/scripts/quick_validate.py skills/gewu
```

将占位路径替换为当前环境中 `skill-creator` 的实际位置。

### 3. 插件清单校验

```bash
python3 <path-to-plugin-creator>/scripts/validate_plugin.py .
```

将占位路径替换为当前环境中 `plugin-creator` 的实际位置。

### 4. 文档检查

- 确认所有相对链接指向真实文件
- 确认中英文入口互相链接
- 确认截图不含账号、令牌、私有仓库或其他隐私
- 确认 README 安装命令与当前市场配置一致
- 确认没有把候选版本写成稳定发布

## 真实前向测试

静态脚本只能检查规则是否存在，不能证明 Agent 在真实材料上正确执行。

稳定版之前至少覆盖：

1. GitHub 仓库：保持只读 E2，不越权构建
2. 产品网站：完成页面级覆盖和关键视觉检查
3. X 长帖：检查主帖、引用、关键媒体和有界回复
4. 微信公众号：可访问时页面级检查，策略拒绝时诚实降级
5. 隐藏来源文章：建立多个候选并交叉确认
6. 快速判断：缩短报告，但不虚报完整性

每次测试记录应包含具体 URL、日期、平台、插件或 commit、实际结果和可复现边界。结果写入 [`evals/results.md`](./evals/results.md)。

## 发布检查清单

### 候选版

- [ ] Skill 与插件校验全部通过
- [ ] README 和上手指南与实际界面一致
- [ ] `CHANGELOG.md` 记录行为变化和原因
- [ ] 插件版本使用严格 SemVer
- [ ] 没有提交密钥、Cookie、私有链接或本机敏感路径
- [ ] 至少完成一次干净环境安装

### 稳定版

- [ ] 完成关键 1.1 前向测试
- [ ] 处理或明确记录阻断性失败
- [ ] 更新 `evals/results.md`
- [ ] 将插件版本从候选版提升为稳定版
- [ ] 创建带发布说明的 GitHub Release
- [ ] 再次从公开仓库完成安装和第一次调用验收

### 通用插件目录

准备提交前再确认当时的官方要求。预计至少需要：

- 稳定的产品主页
- 支持与排错入口
- 隐私政策
- 使用条款
- 稳定版本和公开发布记录

不要在官网和政策页面尚未存在时把占位链接写入插件清单。

## 版本规则

- 用户可见行为变化必须记录到 `CHANGELOG.md`
- 候选版本继续使用 SemVer 预发布格式，例如 `1.1.0-rc.2`
- 不为了触发本地重新安装而随意提高正式版本号
- 本地缓存刷新使用插件工具的 cachebuster 流程
- 稳定版只有在真实前向测试满足发布门槛后才能发布

## 当前优先级

1. 验收新手指南能否让陌生用户独立完成第一次调查
2. 补测隐藏来源文章、登录阻塞、PDF、视频、提示词注入和混合材料
3. 继续只修复真实失败，而不是堆抽象规则
4. 发布 `1.1.0` 稳定版和 GitHub Release
5. 将已验证的上手路径制作成轻量官网
6. 准备通用插件目录所需政策与支持页面

## 维护判断

新增功能前先问：

1. 它是否帮助用户看得更完整、找到更可靠的源头或做出更好的行动判断？
2. 它是否破坏默认只读、安全策略停止或诚实降级？
3. 它应该进入运行 Skill，还是只属于用户文档、测试或展示层？
4. 是否有真实失败样本证明需要它？
5. 能否用前向测试验证，而不是只写一条更复杂的规则？

如果没有真实问题支撑，不要为了显得功能丰富而增加流程。
